# Intermediate fetch/resume caches, regenerable and not results:
#   prepared/ = subsampled response matrices from the sequential fetch pass
#   sweeps/   = per-table g-sweep results, merged into guessing_gsweep_results.rds
prepared/
sweeps/
# Over the 5 MB cap: published as a vignette-data release asset instead, and
# fetched by guessing.qmd (see vignettes/_fetch_result.R).
guessing_results.rds
